export default function validateImpact(values) {

	let errors = {}

	// TODO: VALIDATE

	// // Email errors
	// if (!values.email) {
	// 	errors.email = "Email required"
	// } else if (!isEmailValid(values.email)) {
	// 	errors.email = "Invalid email address"
	// }

	return errors;
}
